# orbit-status

A status CLI for openspec-orbit projects. Walks an openspec/ tree and surfaces what's active, what phase each change is in, what needs attention.

(Project just scaffolded; design is in `openspec/explore/`.)
